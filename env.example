# ==============================================
# BlogCommerce 框架環境變數配置
# ==============================================

# 資料庫配置
# 支援: sqlite, mysql, postgresql
DATABASE_TYPE=sqlite
# SQLite
DATABASE_URL=sqlite:///./blog_shop.db
# MySQL
# DATABASE_URL=mysql+pymysql://username:password@localhost:3306/blogcommerce
# PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/blogcommerce

# JWT 認證配置
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production_must_be_very_long_and_random
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 應用基本配置
APP_NAME=BlogCommerce
APP_VERSION=1.0.0
DEBUG=true
API_V1_PREFIX=/api/v1

# CORS 跨域配置 (JSON陣列格式)
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000", "http://localhost:8080"]

# 檔案上傳配置
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=["jpg", "jpeg", "png", "gif", "webp", "svg"]

# 分頁配置
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# 電商配置
DEFAULT_CURRENCY=TWD
SHIPPING_COST=100.0
FREE_SHIPPING_THRESHOLD=1000.0

# SEO 和網站配置
SITE_NAME=BlogCommerce
SITE_DESCRIPTION=一個現代化的部落格電商系統框架
SITE_URL=http://localhost:3000

# 郵件配置 (可選，用於訂單通知等)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=

# ==============================================
# 前端環境變數 (如需要)
# ==============================================
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_NAME=BlogCommerce
VITE_SITE_URL=http://localhost:3000

# ==============================================
# 部署相關配置
# ==============================================
# 生產環境設定
# DEBUG=false
# BACKEND_CORS_ORIGINS=["https://your-domain.com"]
# SITE_URL=https://your-domain.com 